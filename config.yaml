llm:
  provider: ollama
  base_url: http://localhost:11434/v1
  model: llama3.2
  temperature: 0.7
  max_tokens: 2048

permissions:
  confirm_dangerous: true   # always ask before running shell commands etc.

ui:
  show_tool_calls: true     # print tool name + args before executing
